{
  "description": "ArchDev harness hooks (hook spec 4); generated by `archdev repo hook plugin-hooks-json --harness claude`",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v archdev >/dev/null 2>&1 || exit 0; archdev repo hook start --harness claude --spec 4",
            "timeout": 30
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v archdev >/dev/null 2>&1 || exit 0; archdev repo hook prompt --harness claude --spec 4",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "^(?:Bash|Edit|Write|MultiEdit|NotebookEdit|mcp__.+)$",
        "hooks": [
          {
            "type": "command",
            "command": "command -v archdev >/dev/null 2>&1 || exit 0; archdev repo hook post-tool --harness claude --spec 4",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v archdev >/dev/null 2>&1 || exit 0; archdev repo hook stop --harness claude --spec 4",
            "timeout": 30
          }
        ]
      }
    ],
    "SubagentStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v archdev >/dev/null 2>&1 || exit 0; archdev repo hook subagent-start --harness claude --spec 4",
            "timeout": 30
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v archdev >/dev/null 2>&1 || exit 0; archdev repo hook subagent-stop --harness claude --spec 4",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
